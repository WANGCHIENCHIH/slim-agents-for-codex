# Preset lifecycle

Published preset directories are immutable. Add a new directory for a new model generation; never rewrite or delete older presets silently. Corrections use a new patch-like preset ID. Aliases may move, but installation always records and displays the resolved immutable ID.
